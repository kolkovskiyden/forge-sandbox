# Editorial Review (Prose) — prd.md

**Content:** `_bmad-output/epic/planning-artifacts/prd-ft-1/prd.md` · **reader_type:** humans (also AI-extracted) · **Spelling standard chosen:** American
**Preserved as-is:** frontmatter, transition table, §9.1 table, FR ids, Glossary capitalisation, `[ASSUMPTION: ...]`, `[NOTE FOR PM]`, `[NON-GOAL for MVP]` tags and their contents (no grammar or typo errors found inside any tag). Line numbers refer to the current prd.md.

| Original Text | Revised Text | Changes |
|---------------|--------------|---------|
| (§1, line 22) The app's central job is that you always know whether a Pomodoro is running and you cannot miss its end: | The app's central job is to make sure you always know whether a Pomodoro is running and cannot miss its end: | "job is that [clause]" mismatches noun and complement; an infinitive states the job. Dropped the second "you" for parallelism. |
| (§1, line 22) When the work interval ends, the break starts by itself, and when the break ends, the next Pomodoro starts by itself. | When the Work Interval ends, the Break starts by itself, and when the Break ends, the next Pomodoro starts by itself. | Glossary terms written in lowercase in a sentence that capitalises "Pomodoro"; inconsistent terminology for AI extraction. Same pattern, phrase as Consider: UJ-2 line 46 "his record for the day" → "Day"; §3 line 58 "their Trees and Completed counts" → "their Trees and numbers of Completed Pomodoros" (the phrasing used in FR-20 and FR-21); "day start" (lines 77, 403) vs "Day start hour" (line 370) — pick one; §12 line 423 "(work) and (breaks)" → "(Work Interval) and (Breaks)". Quoted brief text ("automatic transition work → break → work", "a pomodoro is running", "the pomodoro ended a while ago") is left untouched. |
| (UJ-1, line 43) Twenty-five minutes later, still in the IDE, a macOS notification says the Pomodoro is done and a five-minute Short Break has started, and a sound plays. | Twenty-five minutes later, while he is still in the IDE, a macOS notification says the Pomodoro is done and a five-minute Short Break has started, and a sound plays. | Dangling modifier: "still in the IDE" attached to the notification rather than to Denis. |
| (UJ-2, line 46) that Pomodoro is recorded as Interrupted and his record for the day is right after one action. | that Pomodoro is recorded as Interrupted and his record for the Day is correct after a single action. | "is right after one action" can be read temporally ("immediately after") instead of "is correct"; ambiguous for both readers and extraction. |
| (§3, line 60) **Interval** — a running countdown, either a Work Interval or a Break. | Consider: **Interval** — a countdown, either a Work Interval or a Break? | As worded, an Interval is by definition "running", yet FR-2, FR-3 and the Timer State definition refer to a Paused Interval. Dropping "running" removes the apparent contradiction; flagged as a query because it touches a definition. |
| (§3, line 74) an Interval end that happened while the app tab was not visible and focused, and after which the user has not yet acted in the app. | an Interval end that happened while the app tab was not both visible and focused, and after which the user has not yet acted in the app. | "not visible and focused" parses two ways (not-visible AND focused / not (visible AND focused)). The attached assumption shows the second reading is intended; "not both" makes it explicit. |
| (FR-3, lines 118–120) - Resetting a Work Interval ends the Pomodoro as Interrupted; a Pomodoro Record is stored with the actual end time and outcome Interrupted.<br>- Resetting a Break ends the Break; the Timer State becomes Idle with the Work Interval length ready. `[ASSUMPTION: …]`<br>- After Reset the Timer State is Idle and the Current Task can be changed. | - Resetting a Work Interval ends the Pomodoro as Interrupted; a Pomodoro Record is stored with the actual end time and outcome Interrupted.<br>- Resetting a Break ends the Break. `[ASSUMPTION: …]` (unchanged)<br>- After either Reset the Timer State is Idle with the Work Interval length ready, and the Current Task can be changed. | Adjacent-consequence restatement (handed over from the structural pass): "Timer State becomes Idle" appeared in consequence 2 and again in consequence 3. The Idle fact and "Work Interval length ready" now appear once, in the consequence that covers both Reset cases; no fact removed, assumption text untouched. |
| (FR-4, line 125) The Pomodoro Record with outcome Completed is recorded, locally at minimum (FR-18), before the Break is shown as Running, and submitted to the server per FR-16. | The Pomodoro Record with outcome Completed is stored, locally at minimum (FR-18), before the Break is shown as Running, and submitted to the server per FR-16. | "Record is recorded" is tautological; "stored" is the verb the rest of the document uses for Pomodoro Records (§3, FR-3, FR-6, FR-16). |
| (FR-5, line 134) If the Current Task was marked Done during the Break (FR-10 prevents this) or no Current Task exists, the Timer State becomes Idle instead. | Consider: If the Current Task is Done when the Break ends (a state FR-10 should prevent) or no Current Task exists, the Timer State becomes Idle instead? | The sentence states a condition and, in the same breath, that it cannot occur; readers cannot tell whether this is a real branch or a defensive rule. The rewrite keeps the meaning (defensive handling) but makes that intent explicit. Query because it touches requirement logic. |
| (FR-6 Notes, line 145) Ordinary browser behaviour of this kind must not make the timer late or wrong; the concrete mechanisms belong to architecture, see `addendum.md`. | Ordinary browser behavior of this kind must not make the timer late or wrong; the concrete mechanisms belong to architecture (see `addendum.md`). | Comma splice before "see"; parenthetical cross-reference instead. "behaviour" → "behavior" applied here as part of the spelling entry below. |
| (FR-8, line 160) The Task Picker lists Open Tasks only; Done Tasks do not appear in it. | Consider: The Task Picker lists Open Tasks only? | Intra-consequence restatement (other adjacent restatement found): the second clause says the same as the first and as the §3 Task Picker definition. Query because the negative form may be wanted as an explicit test. |
| (§4.3 Description, line 179) The brief names the three channels, tab title, system notification, and sound; this section states what each must achieve, and UX and architecture decide how. | The brief names the three channels (tab title, system notification, and sound); this section states what each must achieve, and UX and architecture decide how. | Comma-delimited appositive reads as a four-item list ("channels, tab title, notification, sound"). Parentheses make the three channels unambiguous. |
| (FR-12, line 195) A macOS Focus mode or Do Not Disturb may suppress the notification; the app does not depend on it as the only signal. The Completion Sound, the tab title, and the return-to-app summary (FR-11, FR-14, FR-15) carry the Interval end on their own. | A macOS Focus mode or Do Not Disturb may suppress the notification, so the app never relies on it alone: the tab title, the Completion Sound, and the return-to-app summary (FR-11, FR-14, FR-15) each carry the Interval end on their own. | Handed over from the structural pass: two sentences for one point, merged into one. Also fixes "depend on it", whose nearest antecedent was "Do Not Disturb", and reorders the list to match the FR references (tab title = FR-11, Completion Sound = FR-14, summary = FR-15). |
| (FR-13, line 202) …a persistent, non-blocking indicator that system notifications are off, with how to enable them in the browser and a button to request permission again; | …a persistent, non-blocking indicator that system notifications are off, with instructions for enabling them in the browser and a button to request permission again; | "with how to enable them" is a nonstandard construction; a noun phrase parallels "a button". |
| (§4.4 Description, line 219) A Completed Pomodoro is never lost because the server was briefly unreachable. | A Completed Pomodoro is never lost merely because the server was briefly unreachable. | Without "merely", the sentence can be read as giving the reason the Pomodoro is never lost. |
| (§4.5 Description, line 243) …so the time invested in a Task is visible at a glance and leads over numbers. | …so the time invested in a Task is visible at a glance and comes before numbers. | "leads over numbers" is not idiomatic; "comes before" matches the Vision's "tables come second". |
| (FR-20, line 253) The Tree is shown with the Current Task during an Interval and next to every Task in the Task List, with its number of Completed Pomodoros. | The Tree is shown with the Current Task during an Interval and next to every Task in the Task List, with the Task's number of Completed Pomodoros. | "its" could refer to the Tree or the Task; made explicit. |
| (§4.6 Description, line 266) Interrupted Pomodoros are shown separately so they are not forgotten and not counted. | Consider: Interrupted Pomodoros are shown separately, so they are not forgotten, but they are not counted? | "not forgotten and not counted" pairs two clauses with opposite intent (a purpose and a restriction) under one "so"; the rewrite separates them. Query because the current wording is understandable on a second read. |
| (§6, line 319) See FR-6: remaining time within 1 second of true, Interval end detected within 2 seconds. | See FR-6: remaining time within 1 second of the true remaining time, Interval end detected within 2 seconds. | "within 1 second of true" is elliptical to the point of reading as a typo. |
| (§9.1, line 352) The priority column says what gives if the learning-project timebox bites. | Consider: The priority column says what is cut first if the learning-project timebox bites? | "what gives" is slang whose meaning is not recoverable for a non-native reader or an extractor; "cut first" echoes the table's own "First to cut" label. Query because it is a voice choice. |
| (spelling, whole document) behaviour (line 145); synchronisation (line 342) — against minimized (140), Realizes (85, 149, 179, 219, 243, 266), Monetization (338), optimize (386) | behavior; synchronization | Mixed British/American spelling; the document is predominantly American (8 American forms vs 2 British), so American is adopted. Only these two words need to change; "toward" (63, 112, 401) and "focused" (22) are already American. One deduplicated entry; the line-145 change is also shown in the FR-6 Notes entry above. |
| (control verbs, lines 85, 106, 109, 116, 172, 323) The user can Start, Pause, Resume, and Reset. / requires a Reset first / can pause … and resume / can reset / the user must Reset first / Start, Pause, Resume, and Reset are reachable | Consider: capitalise Start/Pause/Resume/Reset when naming the control (lines 85, 106, 323, and "press Start" throughout) and use lowercase when describing the action (lines 109, 116); line 172 "must Reset first" would then become "must reset first"? | The control names are capitalised in some verb positions and not in others; a single rule keeps extraction consistent. Query because the distinction may be intentional. |
