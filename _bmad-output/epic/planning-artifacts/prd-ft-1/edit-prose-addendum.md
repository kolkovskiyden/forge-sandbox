# Editorial review (prose) — `addendum.md`

- **Content:** `_bmad-output/epic/planning-artifacts/prd-ft-1/addendum.md`
- **Reader type:** humans (architects, UX designers, and AI agents)
- **Constraints applied:** PRD Glossary capitalization and FR ids left as written; source URLs and "not verified" caveats untouched; American spelling enforced and reported as one deduplicated entry.
- **Skipped:** YAML frontmatter (lines 1-6), headings, `Sources:` lists (including the ` , ` separator spacing, which is treated as intentional URL delimiting).
- **Deliberately not flagged:** the label-then-fragment bullet style in "Options considered and rejected" and the dateline fragments ("Web research, 2026-09-24.") are consistent authorial voice; lowercase "tree"/"pomodoros" when describing comparables' concepts is a correct distinction from the Glossary terms (one exception queried below). The FR-20 reference on line 53 was checked against the PRD and is correct.

| Original Text | Revised Text | Changes |
|---------------|--------------|---------|
| "as summarised in the brief's memlog" (line 15); "a mode-coloured favicon" (line 23); "Behaviour of Forest on tag deletion" (line 28); "the learning goal favours that shape" (line 52) | "as summarized in the brief's memlog"; "a mode-colored favicon"; "Behavior of Forest on tag deletion"; "the learning goal favors that shape" | American spelling per style constraint; four British spellings in one deduplicated entry. All other spellings in the document are already American ("Notification Center" is the macOS product name and stays). |
| "single user; SPA + REST; learning project; out of scope multi-user, mobile, external integrations." (line 15) | "single user; SPA + REST; learning project; out of scope: multi-user, mobile, external integrations." | Added a colon after "out of scope" so the last semicolon item reads as a label followed by its list rather than a run-on noun string. |
| "Product brief FT-2: `...brief.md` (final). Its `addendum.md` holds the competitive landscape digest" (line 14) | "Product brief FT-2: `...brief.md` (final). The brief's `addendum.md`, in the same folder, holds the competitive landscape digest" | "Its" follows a file path, so the pronoun reads as the file's addendum. Naming the brief and its folder removes the ambiguity for readers who resolve the path literally (AI agents especially). |
| "(6 comparables, common complaints)" (line 14); compare "seven features" (line 59), "three per Pomodoro" (line 32), "seven days" (line 43), "five minutes" (line 42) versus "2 seconds" (line 42), "30 seconds" (line 42) | Consider: "(six comparables, common complaints)"? | Number style is mixed. Counts of things are spelled out everywhere except "6 comparables"; spelling it out aligns with the document and the Microsoft baseline (spell out zero through nine). Time values ("2 seconds", "30 seconds") may stay as numerals, which the baseline allows for measurements, so only the count is queried. One deduplicated entry. |
| "needs a mechanism that is not itself throttled: a Web Worker, a server-known end time, or scheduling the Alert independently of the UI loop." (line 42) | "needs a mechanism that is not itself throttled: a Web Worker, a server-known end time, or an Alert scheduled independently of the UI loop." | Made the three list items parallel (noun, noun, noun instead of noun, noun, gerund). |
| "some run the tick in a Web Worker, which is not subject to the intensive throttling." (line 42) | Consider: "some run the tick in a Web Worker, which is not subject to Chrome's intensive throttling."? | "the intensive throttling" uses a definite article for a term that has not been named as such; the earlier sentence describes the behavior but does not call it "intensive throttling". Attributing it to Chrome ties the term to the once-per-minute rule just described. Query, since "intensive throttling" is Chrome's own name for the feature and the author may prefer to keep it bare. |
| "Safari and Firefox only allow the permission prompt inside a user gesture" (line 43) | "Safari and Firefox allow the permission prompt only inside a user gesture" | Moved "only" next to the phrase it restricts ("inside a user gesture"). As written it can be read as "the only thing Safari and Firefox allow". |
| "Hence the request on the first Start and the retry button." (line 43) | Consider: "Hence the PRD requests permission on the first Start and provides a retry button."? | Verbless fragment; "the request" and "the retry button" have no stated owner, so a reader has to infer that these are PRD decisions (FR-13). Query because fragments are part of this document's voice elsewhere. |
| "All three browsers hand web notifications to macOS Notification Center" (line 44) | Consider: "Chrome, Firefox, and Safari all hand web notifications to macOS Notification Center"? | "All three" depends on the browser list in the previous bullet. Each mechanism note is tagged to its own FR and is likely to be read in isolation by the architecture stage, so naming the browsers makes the bullet self-contained. |
| "keeping it derived avoids drift between Tree and statistics." (line 47); compare "the natural dependency for Tree and Statistics" (line 59) | "keeping it derived avoids drift between the Tree and Statistics." | Inconsistent capitalization of the Statistics feature (PRD §4.6): lowercase on line 47, capitalized on line 59. Capitalized to match line 59 and the PRD; added "the" before "Tree" since here it is the Glossary object, not the feature name. One deduplicated entry. |
| "which is exactly the category complaint the product wants to avoid." (line 51) | Consider: "which is exactly the complaint about this app category that the product wants to avoid."? | "category complaint" is a compressed noun pair whose meaning (a complaint common to Pomodoro apps as a category) is not recoverable without the brief. Query; the expansion adds words but no new claim. |
| "Persistence (FR-16 to FR-18) is the natural dependency for Tree and Statistics." (line 59) | "Persistence (FR-16 to FR-18) is the natural dependency of Tree and Statistics." (or: "Tree and Statistics naturally depend on Persistence (FR-16 to FR-18).") | "dependency for X" can be read in either direction (X depends on it, or it depends on X). "Dependency of" / "depend on" states the direction the decomposition stage needs. |
| "PRD §9.1 lists the FR ranges, the core, and the cut order." (line 59) | Consider: "PRD §9.1 lists the FR ranges, the Core tier, and the cut order."? | "the core" is a bare reference to §9.1's Core / Keep / First-to-cut tiering. Naming it as a tier, with the PRD's capitalization, tells the reader what to look for. |
| "TickTick caps pauses at three per Pomodoro" (line 32); compare "pomodoros per day" (line 31) and "one tree per session" (line 27) | Consider: "TickTick caps pauses at three per pomodoro"? | Elsewhere the document correctly lowercases comparables' concepts ("pomodoros", "tree") to distinguish them from the Glossary objects. This instance describes TickTick's session, not this product's Pomodoro. Query only: the Glossary capital may be intended if the author treats the term generically. |
